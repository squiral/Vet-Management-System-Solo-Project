<h2 class="title">Animals</h2>

<div class="animal_info">
<% @animals.each do |animal| %>

  <div class="animal">
    <h3><%= animal.name() %></h3>
    <p>DoB: <%= animal.date_of_birth() %></p>
    <p>Type: <%= animal.type() %></p>
    <p>Vet: <%  if animal.vet_id != nil %>
      <%= animal.vet.name() %>
      <% else %>
       <u>Unassigned</u></p>
      <% end %>
    <p><a href="/animals/<%= animal.id %>"><%=animal.name()%>'s Profile</a></p>
  </div>

<% end %>
</div>
